'use client';

import { useParallax } from '@/hooks';

/**
 * Client-side animation enhancements for the About page.
 * Initializes parallax effects for any parallax elements on the page.
 */
export function AboutPageClient() {
  useParallax();
  return null;
}
